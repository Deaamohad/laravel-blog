<div class="w-10 h-10 bg-blue-100 rounded-full {{ $margin ? 'mt-' . $margin : '' }} flex items-center justify-center">
    <span class="text-blue-600 font-semibold">{{ substr($name, 0, 1) }}</span>
</div>
